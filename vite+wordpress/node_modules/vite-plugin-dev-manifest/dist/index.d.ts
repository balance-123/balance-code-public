import { Plugin } from 'vite';

interface ManifestPluginConfig {
    omitInputs?: string[];
    manifestName?: string;
    delay?: number;
    clearOnClose?: boolean;
}
interface PluginManifest {
    url: string;
    inputs: {
        [key: string]: string;
    };
}
declare const plugin: ({ omitInputs, manifestName, delay, clearOnClose }?: ManifestPluginConfig) => Plugin;

export { ManifestPluginConfig, PluginManifest, plugin as default };
